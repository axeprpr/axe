#!/usr/bin/env bash
set -euo pipefail

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export PYTHONPATH="${script_dir}/../lib${PYTHONPATH:+:${PYTHONPATH}}"
exec python3 -m axe_cli.cli "$@"
